{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-12 col-md-4">
            {{ form_start(registrationForm) }}
            <div class="text-center mb-4">
                <i class="fas fa-user-edit fa-6x my-4"></i>
                <h1 class="mb-4 font-weight-normal">Sign up</h1>
                <p>Create you're account.</p>
            </div>
            <div class="form-label-group mb-3">
                {{ form_row(registrationForm.email, {'attr':{'class':'form-control', 'required':'required', 'autofocus':'autofocus'}}) }}
            </div>
            <div class="form-label-group mb-3">
                {{ form_row(registrationForm.plainPassword, {'attr':{'class':'form-control', 'required':'required', 'autofocus':'autofocus'}}) }}
            </div>
            <button class="btn btn-dark btn-lg btn-block">Register</button>
            <p class="mt-5 mb-3 text-muted text-center">© 2019</p>
            {{ form_errors(registrationForm) }}
            {{ form_end(registrationForm) }}
        </div>
    </div>
{% endblock %}
