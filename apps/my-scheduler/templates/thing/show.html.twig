{% extends 'base.html.twig' %}

{% block title %}Thing{% endblock %}

{% block body %}
    <h1>Thing</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ thing.id }}</td>
            </tr>
            <tr>
                <th>IdentificationNumber</th>
                <td>{{ thing.identificationNumber }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ thing.brand }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ thing.model }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('thing_index') }}">back to list</a>

    <a href="{{ path('thing_edit', {'id': thing.id}) }}">edit</a>

    {{ include('thing/_delete_form.html.twig') }}
{% endblock %}
