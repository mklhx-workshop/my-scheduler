<h3>You ask for password change!</h3>

Hi {{ userName }}! You're ask for password change.

{# example, assuming you have a route named "login" #}
Click here to proceed :  <a href="{{ url('change_password',{'key':token,'email':email}) }}">...</a>.
Thanks!

{# Makes an absolute URL to the /images/logo.png file #}
{#<img src="{{ absolute_url(asset('images/logo.png')) }}">#}